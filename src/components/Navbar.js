import {Navbar} from 'react-bootstrap'
function Header() {
  return <Navbar className="d-flex justify-content-center" bg="dark" variant="dark">
    <Navbar.Brand>
      CTA Train Tracker
    </Navbar.Brand>
  </Navbar>
}

export default Header
